resources:
  jobs:
    mock_job:
      name: MOCK
      schedule:
        quartz_cron_expression: "0 0 0 * * ?"
        timezone_id: "Asia/Ho_Chi_Minh"
        pause_status: "UNPAUSED"
      tasks:
        - task_key: ADS
          notebook_task:
            notebook_path: ../src/mock/mock_daily_sources.ipynb